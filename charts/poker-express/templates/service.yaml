apiVersion: v1
kind: Service
metadata:
  name: {{ include "poker-express.fullname" . }}
  labels:
    {{- include "poker-express.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "poker-express.selectorLabels" . | nindent 4 }}
